7MCh0TJ3XsVYyOQc
https://supabase.com/dashboard/project/xbnkmstqkascpvrrhxrn

cd CDO && git pull
rm -rf CDO && git clone https://github.com/Verbiush/CDO.git
git clone https://github.com/Verbiush/CDO.git
cd CDO
git pull origin main
sudo docker-compose -f docker-compose-aws.yml up -d --build
sudo docker ps
sudo docker-compose -f docker-compose-aws.yml logs -f

sudo docker exec -it cdo_app python3 -c "import sqlite3; conn = sqlite3.connect('/app/data/users.db'); c = conn.cursor(); c.execute(\"SELECT name FROM sqlite_master WHERE type='table'\"); print(c.fetchall()); conn.close()"

# Ver cuánto espacio hay
df -h

# Limpiar imágenes y contenedores Docker sin usar
docker system prune -af

# Ver cuánto liberó
df -h

# Limpiar volúmenes Docker
docker volume prune -f

# Ver qué ocupa más espacio
du -sh ~/* 2>/dev/null | sort -rh | head -10

cd ~/CDO
docker-compose build --no-cache
docker-compose up -d

# Ver qué está usando el puerto 8000
docker ps | grep 8000

# Detener ese contenedor
docker stop cdo_api cdo_app 2>/dev/null


sudo rm -rf /home/ec2-user/CDO/temp_uploads/*
docker system prune -af
df -h

# Limpiar huérfanos y volver a subir
docker-compose down --remove-orphans
docker-compose up -d

cd ~/CDO
git pull origin main
docker-compose down
docker-compose build --no-cache
docker-compose up -d




